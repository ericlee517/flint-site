<% if (is_page() && page.type === 'posts') { %>
	<%- partial('_partial/posts') %>
<% } %>

<% if (is_page() && page.type === 'policy') { %>
	<%- partial('_partial/policy') %>
<% } %>

<% if (is_page() && page.type === 'joinus') { %>
	<%- partial('_partial/joinus') %>
<% } %>

<% if (is_page() && page.type === 'policies') { %>
	<%- partial('_partial/policies') %>
<% } %>

<% if (is_page() && page.type === 'contact') { %>
	<%- partial('_partial/contact') %>
<% } %>

<% if (is_page() && page.type === '404') { %>
	<%- partial('_partial/404') %>
<% } %>

<% if (is_page() && page.type === 'wechat') { %>
	<%- partial('_partial/wechat') %>
<% } %>

<% if (is_page() && page.type === 'services') { %>
	<%- partial('_partial/services') %>
<% } %>